# Base URL of the semantic-search (RAG) API used by /search.
# Defaults to the production deployment when unset.
NEXT_PUBLIC_RAG_API_URL=https://rag.nudaui.dev

# Canonical site URL used for SEO metadata. Defaults to https://nudaui.dev.
# NEXT_PUBLIC_SITE_URL=https://nudaui.dev
